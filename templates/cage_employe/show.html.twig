{% extends 'base.html.twig' %}

{% block title %}CageEmploye{% endblock %}

{% block body %}
    <h1>CageEmploye</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cage_employe.id }}</td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>{{ cage_employe.dateDebut ? cage_employe.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cage_employe_index') }}">back to list</a>

    <a href="{{ path('app_cage_employe_edit', {'id': cage_employe.id}) }}">edit</a>

    {{ include('cage_employe/_delete_form.html.twig') }}
{% endblock %}

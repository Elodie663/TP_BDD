<form method="post" action="{{ path('app_contraction_delete', {'id': contraction.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contraction.id) }}">
    <button class="btn">Delete</button>
</form>

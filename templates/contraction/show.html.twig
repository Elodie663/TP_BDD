{% extends 'base.html.twig' %}

{% block title %}Contraction{% endblock %}

{% block body %}
    <h1>Contraction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contraction.id }}</td>
            </tr>
            <tr>
                <th>DateContraction</th>
                <td>{{ contraction.dateContraction ? contraction.dateContraction|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contraction_index') }}">back to list</a>

    <a href="{{ path('app_contraction_edit', {'id': contraction.id}) }}">edit</a>

    {{ include('contraction/_delete_form.html.twig') }}
{% endblock %}

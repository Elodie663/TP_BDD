{% extends 'base.html.twig' %}

{% block title %}Allee index{% endblock %}

{% block body %}

  <style>
    body {
      background-color: #ffffff;
    }

    h1 {
      text-align: center;
      margin-bottom: 30px;
    }

    .table {
      width: 90%;
      margin: 0 auto 30px auto;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    .table th, .table td {
      border: 1px solid #ccc;
      padding: 10px 15px;
      text-align: center;
    }

    .table thead {
      background-color: #267c60;
      color: white;
    }

    a {
      color: #7bdeaeff;
      text-decoration: none;
      margin: 0 5px;
      font-weight: bold;
    }

    a:hover {
      color: #060303ff;
    }

    .create-button {
      display: block;
      width: fit-content;
      margin: 20px auto;
      padding: 10px 20px;
      background-color: #7bdeaeff;
      color: #ffffffff;
      border-radius: 5px;
      text-align: center;
      text-decoration: none;
    }
  </style>

  <h1>Allee index</h1>

  <table class="table">
    <thead>
      <tr>
        <th>Id</th>
        <th>Numero_allee</th>
        <th>actions</th>
      </tr>
    </thead>
    <tbody>
      {% for allee in allees %}
        <tr>
          <td>{{ allee.id }}</td>
          <td>{{ allee.numeroAllee }}</td>
          <td>
            <a href="{{ path('app_allee_show', {'id': allee.id}) }}">show</a>
            <a href="{{ path('app_allee_edit', {'id': allee.id}) }}">edit</a>
          </td>
        </tr>
      {% else %}
        <tr>
          <td colspan="3">no records found</td>
        </tr>
      {% endfor %}
    </tbody>
  </table>

  <a href="{{ path('app_allee_new') }}" class="create-button">Create new</a>

    <h1>Allee index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Numero_allee</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for allee in allees %}
            <tr>
                <td>{{ allee.id }}</td>
                <td>{{ allee.numeroAllee }}</td>
                <td>
                    <a href="{{ path('app_allee_show', {'id': allee.id}) }}">show</a>
                    <a href="{{ path('app_allee_edit', {'id': allee.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_allee_new') }}">Create new</a>

{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Fonctionnalite{% endblock %}

{% block body %}
    <h1>Fonctionnalite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fonctionnalite.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ fonctionnalite.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ fonctionnalite.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_fonctionnalite_index') }}">back to list</a>

    <a href="{{ path('app_fonctionnalite_edit', {'id': fonctionnalite.id}) }}">edit</a>

    {{ include('fonctionnalite/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Vaccin{% endblock %}

{% block body %}
    <h1>Vaccin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vaccin.id }}</td>
            </tr>
            <tr>
                <th>Nom_vaccin</th>
                <td>{{ vaccin.nomVaccin }}</td>
            </tr>
            <tr>
                <th>Type_vaccin</th>
                <td>{{ vaccin.typeVaccin }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vaccin_index') }}">back to list</a>

    <a href="{{ path('app_vaccin_edit', {'id': vaccin.id}) }}">edit</a>

    {{ include('vaccin/_delete_form.html.twig') }}
{% endblock %}

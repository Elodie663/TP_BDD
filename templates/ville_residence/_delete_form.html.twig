<form method="post" action="{{ path('app_ville_residence_delete', {'id': ville_residence.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ville_residence.id) }}">
    <button class="btn">Delete</button>
</form>

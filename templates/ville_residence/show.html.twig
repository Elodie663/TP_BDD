{% extends 'base.html.twig' %}

{% block title %}VilleResidence{% endblock %}

{% block body %}
    <h1>VilleResidence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ville_residence.id }}</td>
            </tr>
            <tr>
                <th>NomVille</th>
                <td>{{ ville_residence.nomVille }}</td>
            </tr>
            <tr>
                <th>CodePostal</th>
                <td>{{ ville_residence.codePostal }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ ville_residence.pays }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ville_residence_index') }}">back to list</a>

    <a href="{{ path('app_ville_residence_edit', {'id': ville_residence.id}) }}">edit</a>

    {{ include('ville_residence/_delete_form.html.twig') }}
{% endblock %}

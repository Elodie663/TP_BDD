{% extends 'base.html.twig' %}

{% block title %}CarnetSante{% endblock %}

{% block body %}
    <h1>CarnetSante</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ carnet_sante.id }}</td>
            </tr>
            <tr>
                <th>Date_creation</th>
                <td>{{ carnet_sante.dateCreation ? carnet_sante.dateCreation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Observations_generales</th>
                <td>{{ carnet_sante.observationsGenerales }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_carnet_sante_index') }}">back to list</a>

    <a href="{{ path('app_carnet_sante_edit', {'id': carnet_sante.id}) }}">edit</a>

    {{ include('carnet_sante/_delete_form.html.twig') }}
{% endblock %}

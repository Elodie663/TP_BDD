{% extends 'base.html.twig' %}

{% block title %}Vaccination{% endblock %}

{% block body %}
    <h1>Vaccination</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vaccination.id }}</td>
            </tr>
            <tr>
                <th>Date_vaccination</th>
                <td>{{ vaccination.dateVaccination ? vaccination.dateVaccination|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date_prochaine_vaccination</th>
                <td>{{ vaccination.dateProchaineVaccination ? vaccination.dateProchaineVaccination|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vaccination_index') }}">back to list</a>

    <a href="{{ path('app_vaccination_edit', {'id': vaccination.id}) }}">edit</a>

    {{ include('vaccination/_delete_form.html.twig') }}
{% endblock %}
